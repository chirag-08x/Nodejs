// The event loop is what allows Node.js to perform non-blocking(code that doesn't runs in the background likef for loop) I/O operations — despite the fact that JavaScript is single-threaded — by offloading operations to the system kernel whenever possible.
// It basically works same as that of async JS.